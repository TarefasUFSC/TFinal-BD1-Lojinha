{
	"name": "Backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon src/server.js",
		"migrations": "npx knex migrate:latest",
		"seed": "npx knex migrate:rollback --all && npm run migrations && npx knex seed:run"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1",
		"knex": "^0.95.10",
		"sqlite3": "^5.0.2"
	},
	"devDependencies": {
		"nodemon": "^2.0.12"
	}
}
